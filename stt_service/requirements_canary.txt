# NVIDIA Canary STT Production Service Requirements
# Matches your working canarytest environment setup

# Core ML frameworks (your exact working versions)
torch==2.10.0
torchaudio==2.10.0
numpy==2.2.6

# NeMo toolkit - install from GitHub as you did
# Install with: pip install git+https://github.com/NVIDIA/NeMo.git

# Audio processing
soundfile==0.12.1

# FastAPI service
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.22

# Data validation
pydantic==2.5.0

# Required for NeMo (your working setup already has these)
pyannote-core
pyannote-metrics

# Optional: for better async performance
aiofiles==23.2.1